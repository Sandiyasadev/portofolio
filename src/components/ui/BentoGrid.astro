---
interface Skill {
  icon: string;
  title: string;
  description: string;
  type: 'main' | 'side' | 'small' | 'accent';
  stat?: string;
  statLabel?: string;
}

const skills: Skill[] = [
  {
    icon: 'zap',
    title: 'Full-Stack Development',
    description: 'Membangun aplikasi web end-to-end dengan teknologi modern. Dari frontend yang responsif hingga backend yang scalable dan secure.',
    type: 'main'
  },
  {
    icon: 'lock',
    title: 'Clean Code & Best Practices',
    description: 'Menulis kode yang bersih, maintainable, dan well-documented. Mengikuti standar industri dan design patterns.',
    type: 'side'
  },
  {
    icon: 'cpu',
    title: 'Tech Stack',
    description: 'React, Next.js, Astro, Node.js',
    type: 'small',
    stat: 'Modern',
    statLabel: 'Stack Focus'
  },
  {
    icon: 'activity',
    title: 'Experience',
    description: 'Years of building digital products',
    type: 'small',
    stat: '5+',
    statLabel: 'Years Exp'
  },
  {
    icon: 'database',
    title: 'Projects Completed',
    description: 'Explore my portfolio',
    type: 'accent',
    stat: '50+',
    statLabel: 'Projects'
  }
];
---

<section id="skills" class="skills-section">
  <div class="skills-container">
    <!-- Section Header -->
    <div class="skills-header">
      <div class="skills-header-content">
        <span class="section-label">Core Skills</span>
        <h2 class="section-title">The Knowledge Grid</h2>
      </div>
      <p class="skills-header-desc">
        Eksplorasi mendalam pada perpaduan antara kreativitas desain dan presisi engineering.
      </p>
    </div>

    <!-- Bento Grid -->
    <div class="bento-grid">
      <!-- Main Card -->
      <div class="bento-card bento-main">
        <div class="card-gradient"></div>
        <div class="card-content">
          <div class="card-icon-box">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"></polygon>
            </svg>
          </div>
          <h3 class="card-title">
            Building <span class="accent">Digital Products</span> with Modern Technologies
          </h3>
          <p class="card-desc">
            Spesialisasi dalam pengembangan aplikasi web full-stack menggunakan React, Next.js, dan Node.js. Fokus pada performa, aksesibilitas, dan user experience yang exceptional.
          </p>
        </div>
        <div class="card-footer">
          <a href="#projects" class="card-link">
            <span>View Projects</span>
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="5" y1="12" x2="19" y2="12"></line>
              <polyline points="12 5 19 12 12 19"></polyline>
            </svg>
          </a>
        </div>
      </div>

      <!-- Side Card -->
      <div class="bento-card bento-side">
        <div class="card-icon-subtle">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
            <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
          </svg>
        </div>
        <h4 class="card-title-sm">Clean Code & Best Practices</h4>
        <p class="card-desc-sm">
          Menulis kode yang bersih, maintainable, dan well-documented. Mengikuti standar industri, testing practices, dan design patterns.
        </p>
        <div class="card-footer-side">
          <span class="card-label">Development Philosophy</span>
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="arrow-icon">
            <line x1="5" y1="12" x2="19" y2="12"></line>
            <polyline points="12 5 19 12 12 19"></polyline>
          </svg>
        </div>
      </div>

      <!-- Small Cards Row -->
      <div class="bento-card bento-small">
        <div class="small-card-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="4" y="4" width="16" height="16" rx="2" ry="2"></rect>
            <rect x="9" y="9" width="6" height="6"></rect>
            <line x1="9" y1="1" x2="9" y2="4"></line>
            <line x1="15" y1="1" x2="15" y2="4"></line>
            <line x1="9" y1="20" x2="9" y2="23"></line>
            <line x1="15" y1="20" x2="15" y2="23"></line>
            <line x1="20" y1="9" x2="23" y2="9"></line>
            <line x1="20" y1="14" x2="23" y2="14"></line>
            <line x1="1" y1="9" x2="4" y2="9"></line>
            <line x1="1" y1="14" x2="4" y2="14"></line>
          </svg>
        </div>
        <div class="small-card-text">
          <span class="small-card-label">Stack Focus</span>
          <span class="small-card-value">React, Next.js, Node</span>
        </div>
      </div>

      <div class="bento-card bento-small">
        <div class="small-card-icon green">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
          </svg>
        </div>
        <div class="small-card-text">
          <span class="small-card-label">Experience</span>
          <span class="small-card-value">5+ Years Building</span>
        </div>
      </div>

      <!-- Accent Card -->
      <div class="bento-card bento-accent">
        <div class="accent-card-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <ellipse cx="12" cy="5" rx="9" ry="3"></ellipse>
            <path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"></path>
            <path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"></path>
          </svg>
        </div>
        <div class="accent-card-text">
          <span class="accent-card-label">Projects</span>
          <span class="accent-card-value">50+ Completed</span>
        </div>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="5" y1="12" x2="19" y2="12"></line>
          <polyline points="12 5 19 12 12 19"></polyline>
        </svg>
      </div>
    </div>
  </div>
</section>

<style>
  .skills-section {
    padding: 8rem 0;
    background: var(--color-bg-primary);
  }

  .skills-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .skills-header {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    margin-bottom: 5rem;
  }

  @media (min-width: 768px) {
    .skills-header {
      flex-direction: row;
      align-items: flex-end;
      justify-content: space-between;
    }
  }

  .skills-header-content {
    max-width: 600px;
  }

  .section-label {
    display: block;
    font-family: var(--font-mono);
    font-size: 0.625rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.4em;
    color: var(--color-accent);
    margin-bottom: 1rem;
  }

  .section-title {
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .skills-header-desc {
    max-width: 400px;
    font-size: 1.125rem;
    font-weight: 300;
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  /* Bento Grid */
  .bento-grid {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: 1fr;
  }

  @media (min-width: 768px) {
    .bento-grid {
      grid-template-columns: repeat(12, 1fr);
    }
  }

  .bento-card {
    border-radius: 1.5rem;
    transition: all 0.5s ease;
  }

  /* Main Card */
  .bento-main {
    background: var(--color-bg-dark);
    color: white;
    padding: 3rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: 500px;
    position: relative;
    overflow: hidden;
  }

  @media (min-width: 768px) {
    .bento-main {
      grid-column: span 8;
    }
  }

  .bento-main:hover {
    box-shadow: var(--shadow-2xl);
  }

  .card-gradient {
    position: absolute;
    top: 0;
    right: 0;
    width: 66%;
    height: 100%;
    background: linear-gradient(to left, rgba(0, 59, 92, 0.2), transparent);
    opacity: 0;
    transition: opacity 1s ease;
    pointer-events: none;
  }

  .bento-main:hover .card-gradient {
    opacity: 1;
  }

  .card-content {
    position: relative;
    z-index: 10;
  }

  .card-icon-box {
    width: 3.5rem;
    height: 3.5rem;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 2.5rem;
    transition: transform 0.3s ease;
  }

  .bento-main:hover .card-icon-box {
    transform: scale(1.1);
  }

  .card-title {
    font-family: var(--font-serif);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    max-width: 600px;
  }

  .card-title .accent {
    font-style: italic;
    color: var(--color-accent);
  }

  .card-desc {
    font-size: 1.125rem;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.7;
    max-width: 500px;
  }

  .card-footer {
    position: relative;
    z-index: 10;
    margin-top: 3rem;
  }

  .card-link {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 9999px;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    transition: all 0.3s ease;
  }

  .card-link:hover {
    background: white;
    color: var(--color-bg-dark);
    gap: 1.5rem;
  }

  /* Side Card */
  .bento-side {
    background: white;
    border: 1px solid var(--color-border);
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 768px) {
    .bento-side {
      grid-column: span 4;
    }
  }

  .bento-side:hover {
    border-color: var(--color-accent-border);
    box-shadow: var(--shadow-xl);
  }

  .card-icon-subtle {
    width: fit-content;
    padding: 1rem;
    background: var(--color-bg-card);
    border-radius: 1rem;
    margin-bottom: 2rem;
    color: var(--color-accent);
    transition: background 0.3s ease;
  }

  .bento-side:hover .card-icon-subtle {
    background: var(--color-accent-light);
  }

  .card-title-sm {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .card-desc-sm {
    font-size: 0.9375rem;
    font-weight: 300;
    color: var(--color-text-secondary);
    line-height: 1.7;
    flex-grow: 1;
    margin-bottom: 2rem;
  }

  .card-footer-side {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .card-label {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--color-accent);
  }

  .arrow-icon {
    color: var(--color-text-muted);
    transition: transform 0.3s ease;
  }

  .bento-side:hover .arrow-icon {
    transform: translateX(8px);
  }

  /* Small Cards */
  .bento-small {
    background: var(--color-bg-card);
    padding: 2.5rem;
    display: flex;
    align-items: center;
    gap: 1.5rem;
    border: 1px solid transparent;
    box-shadow: var(--shadow-sm);
  }

  @media (min-width: 768px) {
    .bento-small {
      grid-column: span 4;
    }
  }

  .bento-small:hover {
    background: white;
    border-color: var(--color-border);
  }

  .small-card-icon {
    width: 3rem;
    height: 3rem;
    background: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-text-muted);
    box-shadow: var(--shadow-sm);
    flex-shrink: 0;
    transition: all 0.3s ease;
  }

  .bento-small:hover .small-card-icon {
    transform: scale(1.1);
    color: var(--color-accent);
  }

  .small-card-icon.green {
    color: #22c55e;
  }

  .small-card-text {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .small-card-label {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
  }

  .small-card-value {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
  }

  /* Accent Card */
  .bento-accent {
    background: var(--color-accent);
    color: white;
    padding: 2.5rem;
    display: flex;
    align-items: center;
    gap: 1.5rem;
    box-shadow: var(--shadow-lg);
  }

  @media (min-width: 768px) {
    .bento-accent {
      grid-column: span 4;
    }
  }

  .bento-accent:hover {
    box-shadow: var(--shadow-xl);
  }

  .accent-card-icon {
    width: 3rem;
    height: 3rem;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: transform 0.3s ease;
  }

  .bento-accent:hover .accent-card-icon {
    transform: scale(1.1);
  }

  .accent-card-text {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .accent-card-label {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    opacity: 0.7;
  }

  .accent-card-value {
    font-size: 0.875rem;
  }
</style>
